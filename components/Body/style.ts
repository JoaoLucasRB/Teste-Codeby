import styled from 'styled-components';

export const Container = styled.main`
  max-width: 1120px;
  margin: 0 auto;
  margin-bottom: 4.5rem;

  padding: 0.8rem;
  background: var(--grey);
  border-radius: 8px;
`